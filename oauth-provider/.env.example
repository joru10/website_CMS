# Copy to .env and fill in values
# For Render/Netlify, set as environment variables in the dashboard instead.

OAUTH_CLIENT_ID=
OAUTH_CLIENT_SECRET=
OAUTH_REDIRECT_URL=
# Optional (defaults: repo,user)
SCOPE=repo,user
# Optional
PORT=8080
